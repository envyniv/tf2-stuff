unbindall

//https://developer.valvesoftware.com/wiki/List_of_TF2_console_commands_and_variables
//https://developer.valvesoftware.com/wiki/Customizing_Options:_Keyboard
//https://developer.valvesoftware.com/wiki/Bind
//https://wiki.teamfortress.com/wiki/Scripting#List_of_key_names


// WEAPONS //

bind "0" "slot10"
bind "1" "slot1"
bind "2" "slot2"
bind "3" "slot3"
bind "4" "slot4"
bind "5" "slot5"
bind "6" "slot6"
bind "7" "slot7"
bind "8" "slot8"
bind "9" "slot9"
bind "r" "+reload"  //+LALT reloadcfg
bind "f" "+inspect" //+LALT taunt
bind "MOUSE1" "+attack"
bind "MOUSE2" "+attack2"
bind "MOUSE4" "+attack3"
bind "CTRL" "viewmodel_toggle"

// WEAPONS //


// UI //

//i hate that source engine keyboard detection is based on physical key position and not character typed.
//I mean sure, it might be convenient for some, but for others who don't use a US layout, it's annoying as hell
bind "h" "showmapinfo"
bind "g" "cl_trigger_first_notification"
bind "b" "cl_decline_first_notification"
bind "KP_MULTIPLY" "lastinv"
bind "`" "changeclass"              //+LALT = changeteam - for example, i wanted to bind this to <, since i use an IT-IT layout; but of course i cannot.
bind "TAB" "+showscores_info"       //+LALT= callvote
bind "ESCAPE" "cancelselect"        //+LALT disconnect
bind "END" "open_charinfo_direct"

bind "F1" "show_quest_log"
bind "F2" "screenshot"
bind "F3" "abuse_report_queue"
bind "F4" "toggleconsole"

// UI //


// COMMUNICATION //

bind "t" "say_team"     //+LALT = say
bind "v" "+voicerecord" //+LALT = snipercallout
bind "c" "voice_menu_3" //+LALT=niceshot
bind "x" "voice_menu_2" //+LALT=sentryahead
bind "z" "voice_menu_1" //+LALT=moveup
bind "e" "+helpme"      //+LALT = spy callout

// COMMUNICATION //


// ACTIONS //

bind "MOUSE5" "demo_man" //binding to F9 doesn't seem to work. hmm
bind "RALT" "+modifier"
bind "k" "killswitcher"
bind "q" "+use_action_slot_item" //+LALT=dropitem
bind "PAUSE" "pause" // Toggle the server pause state. - from (https://developer.valvesoftware.com/wiki/Console_Command_List)
bind "ALT" "+la_mod" //LEFT ALT AS MODIFIER

//fov manager
bind "MWHEELDOWN" "fovdown"
bind "MWHEELUP" "fovup"

//volume manager, button selection because of keyboard (wanted to matched the etched symbols)
//using the binds means causing a whole second of complete lockup, so i disabled them
//bind "F2" "voldown"
//bind "F3" "volup"

// ACTIONS //


// MOVEMENT //

//jumpsound + null y script
bind "SPACE" "+space"
bind "SHIFT" "+crouch;"

// null-x
bind "W" "+mf" //only thing i can think of when i see "mf" is motherfucker
bind "A" "+ml"
bind "S" "+mb"
bind "D" "+mr"

// MOVEMENT //
